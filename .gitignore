# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Environment variables (CRITICAL - contains private keys and secrets!)
.env
.env.*
!.env.example
!.env.template
server/.env
server/dist
.env.backend
.env.local
.env.production
.env.development

# Private keys - NEVER commit these!
**/escrow-keypair.json
**/*keypair*.json
*.key
*.pem
*.p12
*.pfx

# Vercel (may contain sensitive deployment info)
.vercel

# Lock files (optional - some projects prefer to commit these)
# Uncomment if you want to exclude lock files
# package-lock.json
# yarn.lock
# pnpm-lock.yaml
# bun.lockb
